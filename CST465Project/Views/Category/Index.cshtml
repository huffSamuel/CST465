@model List<Category>
@{
    ViewBag.Title = "Web Store";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Categories</h1>

@Html.Partial("_CategoryLinkList")

@if(User.Identity.IsAuthenticated)
{
    @Html.ActionLink("Add New Category", "Add")
}

